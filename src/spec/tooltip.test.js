"use strict";

const Tooltip = require('../js/tooltip.js');

describe("tooltip unit tests", () => {
  describe("#updateTooltipPosition", () => {
    xtest("base case", () => {
      const mockEvent = {
        pageX: 100,
        pageY: 100
      };
    });
  });
});
